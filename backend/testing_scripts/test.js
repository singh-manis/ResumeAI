import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function run() { try { const recruiter = await prisma.user.findFirst({where:{role:'RECRUITER'}}); const int = await prisma.interview.findFirst({orderBy: {scheduledAt: 'desc'}}); if(int) { await prisma.interview.update({where:{id:int.id},data:{scheduledAt: new Date(Date.now() + 86400000)}}); } const loginRes = await fetch('http://localhost:3002/api/auth/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email: recruiter.email, password: 'Demo123!' }) }); const loginData = await loginRes.json(); const token = loginData.accessToken; const intRes = await fetch('http://localhost:3002/api/interviews?upcoming=true', { headers: { 'Authorization': 'Bearer ' + token } }); const intDataObj = await intRes.json(); console.log('Interviews Status:', intRes.status, JSON.stringify(intDataObj)); } catch (e) { console.error('Script error:', e); } finally { await prisma.(); } } run();
